<%- include('../../partials/header') %>

    <header class="jumbotron">
        <!-- <div class="container"> -->
        <h1>Welcome to the Get-Outside (GO) Camping Blog!</h1>
        <p>View our hand-picked campgrounds from all over the world</p>
        <p>
            <a class="btn btn-primary btn-lg" href="/camps/new">Add New Campground</a>
            <% if (!user) { %>
                <a href="/auth/google">Login with Google to Add New Campgrounds</a>
                <% } %>
        </p>
        <!-- </div> -->
    </header>

    <% context.camps.forEach((camp, idx) => { %>
        <div class="card row text-center" style="display: flex; flex-wrap: wrap;">
            <%console.log('camp', camp);%>
                <div class="col-md-3 col-sm-6">
                    <div class="thumbnail">
                        <img src="<%= camp.campDbImage %>">
                        <div class="caption">
                            <h3 class="left top">
                                <%= camp.campDbName %>
                            </h3>
                            <h4 class="right bottom">$
                                <%= camp.campDbPrice %>
                                    /nt</h4>
                            <a href="/camps/<%= camp._id %>" class="right btn btn-primary">More Info</a>
                        </div>
                        <br>
                        <div>
                        </div>
                    </div>
                </div>
        </div>
        <% }); %>

            <%- include('../../partials/footer') %>